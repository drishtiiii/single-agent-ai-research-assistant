
services:
  research-api:
    build: .
    container_name: single-agent-ai-research-assistant

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./reports:/app/reports
      - ./data:/app/data

    restart: unless-stopped
